/**
 * Пул ЛЁГКОГО режима — прежний словарь игры целиком.
 *
 * Отдельный модуль-обёртка нужен затем, чтобы все пулы подключались
 * единообразно (`import('./pool.<id>.js')` → `LINES`) и попадали в
 * собственные чанки сборки.
 */

import { RAW_LINES } from '../dictionary.ru.js';

export const LINES = RAW_LINES;
